# 🛹 Skateraded — WebGL Shader Preview App

This project is a **Vite + React + WebGL2** application for rendering shader-based visual previews.
Built and configured for **Vercel** and **Netlify** deployments.

## 🚀 Quick Start

```bash
npm install
npm run dev
```
Then open [http://localhost:5173](http://localhost:5173)

## 🧱 Build
```bash
npm run build
```

## ☁️ Deploy
### Vercel
Just connect your GitHub repo at https://vercel.com/import — configuration is auto-detected.

### Netlify
Drag & drop this folder to https://app.netlify.com/drop or use CLI:
```bash
netlify deploy --prod
```

## ⚙️ Environment Variables
Set your custom defaults in `.env` (see `.env.example`).
